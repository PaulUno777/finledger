# Dev-only profile for local docker-compose (not for production).

spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/finledger
    username: finledger
    password: finledger

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  data:
    redis:
      host: localhost
      port: 6379
