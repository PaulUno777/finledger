# Dev-only profile for local docker-compose (not for production).

spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/finledger
    username: finledger_app
    password: finledger

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    user: finledger
    password: finledger

  data:
    redis:
      host: localhost
      port: 6379
